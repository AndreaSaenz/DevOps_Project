const {} = require("../src/controllers/User");
describe("Test user endpoints", () => {
  it("should all the register service", async () => {
    const newUser = {
      userName: "JaneDoe",
      email: "janeDoe@example.test",
      password: "janespass",
    };
  });
});
